name: Rent Reminders

on:
  schedule:
    - cron: '0 8 * * *' # every day at 08:00 UTC
  workflow_dispatch: {}

jobs:
  trigger-reminders:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger rent reminders
        uses: actions/http-client@v1
        with:
          url: ${{ secrets.SITE_URL }}/api/rents
          method: PATCH
          headers: '{"Content-Type":"application/json","x-cron-secret":"${{ secrets.RENT_REMINDER_SECRET }}"}'
          body: '{"action":"triggerReminders","daysBefore":3}'
